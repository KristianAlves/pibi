    <!-- Navigation -->
    <nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
        <div class="container">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <a class="navbar-brand" href="#">Comercial Pibi</a>
            </div>
            <!-- Collect the nav links, forms, and other content for toggling -->
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <ul class="nav navbar-nav">
                    <li>
                        <a href="#">About</a>
                    </li>
                    <li>
                        <a href="#">Services</a>
                    </li>
                    <li>
                        <a <%= link_to "Contacts", new_site_send_mail_path, remote: true %></a>
                    </li>
                </ul>
                <ul class="nav navbar-nav pull-right">
                    <% if member_signed_in? %>
                            <li>
                                <%= link_to current_member.email, site_home_index_path %>
                            </li>
                            <li>
                                <%= link_to t('site.profile'), site_home_index_path %>
                            </li>
                            <li>
                                <%= link_to t('site.logout'), destroy_member_session_path, method: :delete %>
                            </li>

                    <% else %>
                            <li>
                                <%= link_to t('site.login'), new_member_session_path %>
                            </li>
                            <li>
                                <%= link_to t('site.registration'), new_member_registration_path %>
                            </li>
                    <% end %>
                </ul>
            </div>
            <!-- /.navbar-collapse -->
        </div>
        <!-- /.container -->
    </nav>

<%= render partial: 'layouts/site_shared/send_mail' %>