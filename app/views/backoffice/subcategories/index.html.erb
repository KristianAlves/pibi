    <h1 class="page-header"><%= t('backoffice.subcategory') %></h1>

        <div class="panel panel-default">
            <div class="panel-heading">
                <%= t('backoffice.show_subcategory') %>
                <%= link_to new_backoffice_subcategory_path, class:"btn btn-success btn-circle pull-right", style:"margin-top: -5px" do %>
                    <i class="fa fa-plus"></i>
                <% end %>
            </div>
            <div class="panel-body">
                <div class="table-responsive">
                    <table class="table table-striped table-bordered table-hover">
                        <thead>
                            <tr>
                                <th style="width: 50px">#</th>
                                <th><%= t('backoffice.subcategory') %></th>
                                <th style="width: 300px"> <%= t('backoffice.belong_category') %></th>
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            <% @subcategories.each do |subcategory| %>
                                <tr>
                                    <td><%= subcategory.id %></td>
                                    <td><%= subcategory.title %></td>
                                    <td><%= subcategory.category.description %></td>
                                    <th style="width: 90px"> </style>
                                            <%= link_to edit_backoffice_subcategory_path(subcategory), class: "btn btn-primary btn-circle" do %>
                                            <i class="fa fa-edit"></i>
                                        <% end %>
                                        <%= link_to backoffice_subcategory_path(subcategory), method: :delete, class:"btn btn-danger btn-circle",
                                                            data: {confirm: "Você tem certeza que deseja excluir <b>(#{subcategory.title})</b>?"} do %>
                                            <i class="fa fa-trash-o"></i>
                                        <% end %>
                                    </th>
                                </tr>
                            <% end %>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>