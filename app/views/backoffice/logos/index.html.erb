<h1 class="page-header">Logo</h1>

<div class="panel panel-default">
    <div class="panel-heading">
      Show Logo
      <%= link_to new_backoffice_logo_path, class:"btn btn-success btn-circle pull-right", style:"margin-top: -5px" do %>
          <i class="fa fa-plus"></i>
      <% end %>
    </div>
    <div class="panel-body">
      <div class="table-responsive">
          <table class="table table-striped table-bordered table-hover">
              <thead >
                  <tr>
                      <th>Logo</th>
                      <th></th>
                  </tr>
              </thead>
              <tbody>
                  <% @logos.each do |logo| %>
                      <tr>
                      <td style: align="center"><%= image_tag logo.picture.url(:thumb) %></td>
                      <th style="width: 90px"> </style>
                          <%= link_to edit_backoffice_logo_path(logo), class: "btn btn-primary btn-circle" do %>
                              <i class="fa fa-edit"></i>
                          <% end %>
                          <%= link_to backoffice_logo_path(logo), method: :delete, class: "btn btn-danger btn-circle",
                           data: {confirm: "Are you sure you want to delete <b>(#{logo.title})</b>?"} do %>
                              <i class="fa fa-trash-o"></i>
                          <% end %>
                      </th>
                      </tr>
                  <% end %>
              </tbody>
          </table>
      </div>
    </div>
</div>