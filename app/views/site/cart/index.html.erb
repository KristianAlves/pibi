<!DOCTYPE html>
<html lang="en">

    <head>

        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <meta name="description" content="">
        <meta name="author" content="">

      <%= stylesheet_link_tag    'application', media: 'all' %>


      <%= csrf_meta_tags %>



    <% if @carts_itens == 0 %>
        <div class= "text-center">
            <h3> Seu carrinho está vazio </h3>
        </div>
    <% else %>
<h1>Carrinho de Orçamento</h1>
            <div class= "text-right">
                <%= link_to site_home_index_path, class:"btn btn-success" do%>
                    <i class="fa fa-shopping-cart  fa-fw"></i> Continuar Orçando
                <% end %>
            </div>

                <div class="panel-body">
                    <div class="table-responsive">
                        <table class="table table-striped table-bordered table-hover">
                            <thead>
                                <tr>
                                    <th>Produtos</th>
                                    <th>Quantidade</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                            <% @carts.each do |cart| %>
                                <tr>
                                    <td><%= cart.product.title %></td>
                                    <td style="width: 100px"><%= best_in_place cart, :quantity, url: site_cart_path(cart.id) %></td>
                                    <td style="width: 50px"> </style>
                                          <%= link_to site_cart_path(cart.id), method: :delete, class:"btn btn-danger btn-circle",
                                           data: {confirm: "Deseja realmente excluir este item?"} do %>
                                              <i class="fa fa-trash-o"></i>
                                          <% end %>
                                    </td>
                                </tr>

                                <% end %>
                            </tbody>
                        </table>
                    </div>
                    <!-- /.table-responsive -->
                </div>

                                    <div class= "text-right">
                                    <%= link_to new_site_send_mail_member_path, class: "btn btn-success  fa fa-check",
                                            remote: true do %>
                                        Finalizar orçamento
                                        </button>
                                        <% end %>
                                    </div>

    <% end %>

<%= render partial: 'site/cart/cart_modal' %>